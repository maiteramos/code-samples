# Code Samples

Small C# and C++ code samples.

This repository contains a simple matrix string formatter implemented in both C# and C++.

The algorithm builds a string from a matrix using a zig-zag row order:

- Even rows are read from left to right.
- Odd rows are read from right to left.
